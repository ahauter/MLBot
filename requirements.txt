# Include everything the framework requires
# You will automatically get updates for all versions starting with "1.".
rlbot==1.*
rlbottraining

# Scenario builder & visualiser
pyyaml
matplotlib

# ML framework — PyTorch avoids the protobuf/numpy conflicts that TensorFlow
# has with RLBot's dependency pins.
torch

# Fast CPU-based RL simulation (no live game required for training).
# rocketsim provides the C++ physics engine with Windows/Linux/macOS wheels on PyPI.
# rlgym-sim is not on PyPI — install from GitHub (rocketsim is pulled in automatically,
# but listed explicitly here so pip can cache the wheel on repeated installs).
rocketsim
rlgym-sim @ git+https://github.com/AechPro/rocket-league-gym-sim@main

# Replay collection: HTTP client for ballchasing.com API + rlgym-compatible replay parser
requests
rlgym-tools
pandas
pyarrow

gymnasium

# Low-latency gamepad input for HumanProxyBot (Linux /dev/input/ direct access)
evdev

# Experiment tracking and hyperparameter tuning
wandb
optuna

# This will cause pip to auto-upgrade and stop scaring people with warning messages
pip
